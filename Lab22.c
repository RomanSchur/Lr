#include <stdio.h>

int main() {
    char Alphabet;

    for (Alphabet = 'A'; Alphabet <= 'Z'; Alphabet++) {
        printf("%c ", Alphabet);
    }

    printf("\n");
	
	 for (Alphabet = 'a'; Alphabet <= 'z'; Alphabet++) {
        printf("%c ", Alphabet);
    }

    printf("\n");

    return 0;
}
